<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homefun.modules.media.mapper.MediaCategoryMapper">

    <select id="selectCategoryList" resultType="com.homefun.modules.media.domain.MediaCategory">
        SELECT * FROM media_category WHERE del_flag = '0'
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY sort
    </select>

    <select id="selectCategoryById" resultType="com.homefun.modules.media.domain.MediaCategory">
        SELECT * FROM media_category WHERE id = #{id}
    </select>

    <insert id="insertCategory" parameterType="com.homefun.modules.media.domain.MediaCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO media_category (
            name, icon, sort, status,
            create_by, create_time, remark
        ) VALUES (
            #{name}, #{icon}, #{sort}, #{status},
            #{createBy}, NOW(), #{remark}
        )
    </insert>

    <update id="updateCategory" parameterType="com.homefun.modules.media.domain.MediaCategory">
        UPDATE media_category SET
            name = #{name},
            icon = #{icon},
            sort = #{sort},
            status = #{status},
            update_time = NOW()
        <if test="remark != null">
            , remark = #{remark}
        </if>
        WHERE id = #{id}
    </update>

    <update id="deleteCategoryById">
        UPDATE media_category SET del_flag = '1' WHERE id = #{id}
    </update>

</mapper>
