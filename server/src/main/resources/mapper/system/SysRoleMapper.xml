<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homefun.modules.system.mapper.SysRoleMapper">

    <select id="selectRoleList" resultType="com.homefun.modules.system.domain.SysRole">
        SELECT * FROM sys_role WHERE del_flag = '0'
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="roleKey != null and roleKey != ''">
            AND role_key = #{roleKey}
        </if>
        ORDER BY role_sort
    </select>

    <select id="selectRolesByUserId" resultType="com.homefun.modules.system.domain.SysRole">
        SELECT r.* FROM sys_role r
        LEFT JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE r.del_flag = '0' AND ur.user_id = #{userId}
    </select>

    <select id="selectRoleById" resultType="com.homefun.modules.system.domain.SysRole">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <insert id="insertRole" parameterType="com.homefun.modules.system.domain.SysRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (
            role_name, role_key, role_sort, status,
            create_by, create_time, remark
        ) VALUES (
            #{roleName}, #{roleKey}, #{roleSort}, #{status},
            #{createBy}, NOW(), #{remark}
        )
    </insert>

    <update id="updateRole" parameterType="com.homefun.modules.system.domain.SysRole">
        UPDATE sys_role SET
            role_name = #{roleName},
            role_key = #{roleKey},
            role_sort = #{roleSort},
            status = #{status},
            update_by = #{updateBy},
            update_time = NOW()
        <if test="remark != null">
            , remark = #{remark}
        </if>
        WHERE id = #{id}
    </update>

    <delete id="deleteRoleById">
        UPDATE sys_role SET del_flag = '1' WHERE id = #{id}
    </delete>

    <select id="countRoleByMenuId" resultType="int">
        SELECT COUNT(1) FROM sys_role_menu WHERE menu_id = #{menuId}
    </select>

</mapper>
